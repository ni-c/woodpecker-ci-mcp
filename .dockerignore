# Not optional: without it `docker build` can fail while merely READING the build
# context if any file is root-owned 0600 — and it keeps secrets and state away
# from the daemon. `docs` in particular carries its own npm project, which would
# otherwise be copied into every build.
node_modules
dist
coverage
test
docs
data
.git
.github
.env
*.log
docker-compose*.yml
