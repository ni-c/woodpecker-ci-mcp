{
  "$comment": "Copy for docs/public/og.png, the GitHub social preview and the OpenGraph card. Rendered by scripts/gen-assets.mjs. Tagline and badges are split by hand because the renderer does not wrap text. \"title\" overrides the heading — package.json's name reads as @ni-c/woodpecker-ci-mcp, but the card should say the repository name.",
  "title": "woodpecker-ci-mcp",
  "tagline": [
    "Read Woodpecker CI pipelines and logs,",
    "and drive builds, secrets and crons"
  ],
  "badges": ["71 tools", "MIT", "Node ≥22"]
}
