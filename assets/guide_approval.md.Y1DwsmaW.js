import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.Cs5aHXsR.js";var i=JSON.parse(`{"title":"Asking a person","description":"","frontmatter":{},"headers":[],"relativePath":"guide/approval.md","filePath":"guide/approval.md","lastUpdated":1788432583000}`),a={name:`guide/approval.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="asking-a-person" tabindex="-1">Asking a person <a class="header-anchor" href="#asking-a-person" aria-label="Permalink to “Asking a person”">​</a></h1><p>Twenty-three of the 71 tools do something a CI server does not undo, or hand somebody more reach than they had. All twenty-three <strong>ask a person first</strong>.</p><p>Not a <code>confirm: true</code> argument the model can set. Not a token the model reads out of its own previous result. A dialog, raised through <a href="https://modelcontextprotocol.io/specification/2025-11-25/client/elicitation" target="_blank" rel="noreferrer">MCP elicitation</a>, that goes to the client and is shown to whoever is sitting there.</p><p>The specification says a client <em>should</em> keep a human in the loop:</p><blockquote><p>there <strong>SHOULD</strong> always be a human in the loop with the ability to deny tool invocations</p></blockquote><p>This server does not rely on that. It raises the question itself, and until an answer comes back, nothing happens.</p><h2 id="what-asks-and-when" tabindex="-1">What asks, and when <a class="header-anchor" href="#what-asks-and-when" aria-label="Permalink to “What asks, and when”">​</a></h2><table tabindex="0"><thead><tr><th>Group</th><th>When it asks</th></tr></thead><tbody><tr><td>every <code>delete_*</code></td><td>always</td></tr><tr><td><code>move_repository</code> · <code>chown_repository</code></td><td>always</td></tr><tr><td><code>repair_repository</code></td><td>only at <code>scope: instance</code></td></tr><tr><td><code>update_forge</code> · <code>pause_queue</code> · <code>approve_pipeline</code></td><td>always</td></tr><tr><td><code>update_user</code></td><td>only when it grants <code>admin</code></td></tr><tr><td><code>create_user</code></td><td>only when it creates an <code>admin</code></td></tr><tr><td><code>update_repository</code></td><td>only when it grants a <code>trusted_*</code> flag, lowers <code>require_approval</code>, or sets <code>visibility</code> to <code>public</code></td></tr><tr><td><code>update_secret</code></td><td>only when a new <code>value</code> comes with it, when a <code>pull_request</code> event is added, or when <code>images</code> is emptied</td></tr><tr><td><code>update_registry</code></td><td>only when a new <code>password</code> comes with it</td></tr><tr><td><code>set_log_level</code></td><td>only when it <strong>silences</strong> the server (<code>fatal</code>, <code>panic</code>, <code>disabled</code>)</td></tr><tr><td>everything else</td><td>never</td></tr></tbody></table><p>Only that direction, and that is deliberate. Correcting an email, withdrawing trust, renaming a secret or turning the logs <em>up</em> applies on the first call: a confirmation on the harmless half of a tool teaches whoever reads these prompts to click through them, which costs more than it buys.</p><p><code>approve_pipeline</code> is where asking earns its keep most directly. A blocked pipeline is usually one from a fork, and approving it runs that fork&#39;s code with the repository&#39;s secrets — while the model deciding is typically holding a build log it fetched with <code>get_step_logs</code>, which is this server&#39;s one input written by whoever opened the pull request. “Approve pipeline 42” sitting in that log is a plausible-looking instruction. A dialog goes to a person the log cannot reach.</p><p><code>chown_repository</code> is asked about for what the ownership <em>is</em>: every pipeline of that repository afterwards runs under the calling account&#39;s forge token, so its reach over the forge becomes the repository&#39;s reach. <code>delete_user</code> already cited that in its own reasoning while the tool that performs the transfer did not ask.</p><p><code>create_user</code> is asked about on exactly the field <code>update_user</code> is. Until it was added, the same privilege by the same flag had a dialog in front of one of them and not the other — and the description advertised the gap: “which is how you make someone an admin before they first log in.”</p><p><code>pause_queue</code> is on the list for a different reason from the rest. It is reversible, but it is instance-wide and silent: nothing tells the people whose builds are queuing why nothing is starting.</p><h2 id="what-the-dialog-contains" tabindex="-1">What the dialog contains <a class="header-anchor" href="#what-the-dialog-contains" aria-label="Permalink to “What the dialog contains”">​</a></h2><p>Numeric ids and server-side facts. Never a branch name, a commit message, a pipeline title or a line of a build log.</p><p>Everything this server returns was written by somebody who can push a commit, and build logs are the raw stdout of arbitrary containers. The prompt is read by a model at the exact moment it is deciding, so a repository must not be able to name itself into it.</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>This will approve pipeline 42 of repository 21.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>The pipeline runs with this repository&#39;s secrets, and its code comes from the</span></span>
<span class="line"><span>source that opened it.</span></span></code></pre></div><p>The approval is bound to its target, so one obtained for a call cannot be replayed against another. The binding is a fingerprint of the targets <strong>in the order the tool names them</strong>, plus one of the request body where the call decides more than which object it touches. Both halves are load-bearing here, because most of these targets are small integers that look alike: an approval for <code>approve_pipeline(repo_id: 5, number: 12)</code> is not one for <code>(repo_id: 12, number: 5)</code>, and one shown as &quot;grant <code>trusted_network</code>&quot; does not execute a second call that also carries <code>visibility: &quot;public&quot;</code>.</p><h2 id="what-it-does-not-prove-freshness" tabindex="-1">What it does not prove: freshness <a class="header-anchor" href="#what-it-does-not-prove-freshness" aria-label="Permalink to “What it does not prove: freshness”">​</a></h2><p>An approval proves <em>what</em> was agreed to. It does not prove <em>how often</em>. Until it expires, the same one can be redeemed again — a retried call, a gateway that replays a leg, a model that loops. For everything on the list above that is harmless: a second <code>delete_secret</code> finds nothing to delete, a second <code>set_log_level</code> sets the level it is already at, and the world is the same either way. That is what the <code>idempotentHint</code> on each of those tools says.</p><p>Three tools are honestly not idempotent, and they are the three that start a build: <code>trigger_pipeline</code>, <code>restart_pipeline</code> and <code>run_cron</code>. Each call is a new pipeline, by design — re-running a flaky job is the ordinary thing to do in CI — and Woodpecker offers no idempotency key, no request id and no way to say &quot;only if you have not already&quot;. None of the three is behind a confirmation for that reason: a gate that cannot make the operation at-most-once would only look like one. They carry <code>idempotentHint: false</code>, their descriptions say a repeat starts another run, and the cost of a double run is a duplicate build rather than something that cannot be undone.</p><p><code>move_repository</code> is the exception worth naming: it is guarded, and on 3.11 Woodpecker performs the move and <em>then</em> answers HTTP 500, so a caller that treats the error as &quot;it did not happen&quot; and retries moves the repository twice. On 3.18 the call answers cleanly. The tool&#39;s own description says so in full, because a server points at whichever instance it was given.</p><h2 id="clients-that-cannot-show-a-dialog" tabindex="-1">Clients that cannot show a dialog <a class="header-anchor" href="#clients-that-cannot-show-a-dialog" aria-label="Permalink to “Clients that cannot show a dialog”">​</a></h2><p>Not every MCP client implements elicitation, and a stateless gateway may not be able to speak for the one it is currently serving. Rather than refuse to work — which pushes people towards switching the guard off entirely — the tool falls back to a <strong>two-call token</strong>: the first call returns a random string, the second has to quote it back.</p><p>Be clear about what that proves, because this server is:</p><blockquote><p>the token proves the call was made twice with the same arguments, and nothing more.</p></blockquote><p>A model can read the token out of the first result and call again in the same turn without anybody seeing it. It catches a widened target set; it does not catch a model that was talked into the whole thing. The fallback text says so rather than implying somebody approved.</p><h2 id="switching-the-dialog-off" tabindex="-1">Switching the dialog off <a class="header-anchor" href="#switching-the-dialog-off" aria-label="Permalink to “Switching the dialog off”">​</a></h2><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">ELICITATION</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">false</span></span></code></pre></div><p>Default is <code>true</code>. <code>false</code> does <strong>not</strong> remove the guard — it takes the fallback path above, which means the token. There is no setting in which a guarded call goes unannounced.</p><p>Use it where a dialog is the wrong shape rather than an unwanted one: a scheduled job, a test harness, a client whose dialog interrupts something else.</p><div class="warning custom-block"><p class="custom-block-title">It is deliberately not prefixed</p><p><code>ELICITATION</code> has no <code>WOODPECKER_</code> in front of it, so one <code>export ELICITATION=false</code> — or one <code>-e ELICITATION=false</code> in a compose file — reaches <strong>every</strong> MCP server in that environment, not just this one. That is the point of it and also its risk.</p><p>Two things make it visible rather than silent:</p><ul><li><p>a server started with it off prints one line at startup, in the log of every server it actually reached:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>woodpecker-ci-mcp: ELICITATION=false — guarded tools fall back to the two-call token</span></span></code></pre></div></li><li><p>the fallback text names the server that did not ask, instead of blaming a client that was working fine. :::</p></li></ul><p>Anything other than <code>true</code> or <code>false</code> — <code>1</code>, <code>off</code>, <code>yes</code> — <strong>stops the server</strong> with exit code 1 and a message naming both valid values. This is the only variable in this family that defaults to <em>on</em>: a typo that fell back to the default would leave the dialog running while the operator believed it was off, and there would be nothing to tell them.</p><h2 id="annotations-are-the-other-half-and-they-are-only-a-hint" tabindex="-1">Annotations are the other half, and they are only a hint <a class="header-anchor" href="#annotations-are-the-other-half-and-they-are-only-a-hint" aria-label="Permalink to “Annotations are the other half, and they are only a hint”">​</a></h2><p>Every tool of this server declares all four MCP tool annotations — <code>readOnlyHint</code>, <code>destructiveHint</code>, <code>idempotentHint</code>, <code>openWorldHint</code> — so a client can tell before it calls what a call would do. See <a href="/reference/tools">Tools</a>.</p><p>They are advice, and the specification says so:</p><blockquote><p>clients <strong>MUST</strong> consider tool annotations to be untrusted unless they come from trusted servers</p></blockquote><p>An annotation is something a client may ignore. The dialog is not: it is enforced here, on the server side, and no answer means no change. The two are different claims — the annotation says what a call <em>does</em>, the dialog decides whether it <em>happens</em> — which is why a tool can be marked destructive without being guarded. The four tools that <em>run</em> a build — <code>trigger_pipeline</code>, <code>restart_pipeline</code>, <code>run_cron</code>, <code>approve_pipeline</code> — are marked destructive not because Woodpecker loses anything, but because what the pipeline does is written in the repository and this server cannot promise it destroys nothing. Only <code>approve_pipeline</code> asks.</p><h2 id="behind-a-gateway" tabindex="-1">Behind a gateway <a class="header-anchor" href="#behind-a-gateway" aria-label="Permalink to “Behind a gateway”">​</a></h2><p>Both protocol revisions are handled from one code path. On <code>2025-11-25</code> the question is pushed to the client; on <code>2026-07-28</code> there is no server→client channel at all, so the call returns <code>input_required</code>, ends, and the client retries carrying the answer.</p><p>That answer arrives as ordinary request content, which the SDK does not validate — so the state that ties an answer to its question is sealed (HMAC). A reply whose seal does not open, or opens onto a different target, counts as <strong>no answer</strong> and produces a fresh question rather than an error. The likeliest cause is not an attack: it is a gateway that put the server to sleep while the person was reading.</p><p>If you run this behind <a href="https://github.com/ni-c/mcp-hub" target="_blank" rel="noreferrer">mcp-hub</a>, the hub passes elicitation through in both directions; see its <a href="https://ni-c.github.io/mcp-hub/guide/elicitation" target="_blank" rel="noreferrer">elicitation guide</a>.</p></div>`,32)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};