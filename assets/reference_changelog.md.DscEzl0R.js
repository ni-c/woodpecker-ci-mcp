import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.Cs5aHXsR.js";var i=JSON.parse(`{"title":"Changelog","description":"","frontmatter":{},"headers":[],"relativePath":"reference/changelog.md","filePath":"reference/changelog.md","lastUpdated":1787828411000}`),a={name:`reference/changelog.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to “Changelog”">​</a></h1><h2 id="_0-2-0-2026-09-03" tabindex="-1"><a href="https://github.com/ni-c/woodpecker-ci-mcp/releases/tag/v0.2.0" target="_blank" rel="noreferrer">0.2.0</a> - 2026-09-03 <a class="header-anchor" href="#_0-2-0-2026-09-03" aria-label="Permalink to “0.2.0 - 2026-09-03”">​</a></h2><h3 id="added" tabindex="-1">Added <a class="header-anchor" href="#added" aria-label="Permalink to “Added”">​</a></h3><ul><li><p>Every tool declares an <code>outputSchema</code> and answers with <code>structuredContent</code> beside the text block. A client no longer has to parse prose to use a result — which seventeen of them made unavoidable, since they answered with a sentence. The sentence stays, in the text block. <code>get_step_logs</code> keeps its rendered header-plus-log there too, and states the exit code, the line count and the output as fields.</p><p>The tools that report pushed content carry <code>untrusted: true</code> and <code>source: &quot;woodpecker&quot;</code> as fields, not only as a preamble in the text. Branch names, commit messages, pipeline titles and above all build logs are written by whoever can push, so a client that reads the structured half must not get them unframed. The list follows the call sites.</p><p>Woodpecker&#39;s objects are described as open objects with the top-level keys this server builds — the upstream Go models change what they serialize between releases, and a strict shape would turn that into a failed call.</p></li><li><p>The tools that need a confirmation now <strong>ask the user</strong>, on clients that can show a prompt. The two-call <code>confirm_token</code> remains for clients that cannot, so nothing that works today stops working — but where a person can be asked, one is, instead of a token that only proves the same call was made twice.</p></li><li><p><strong>Two more places ask</strong>, and both were gaps the code itself pointed at.</p><p><code>create_user</code> takes an <code>admin</code> flag and did not ask, while <code>update_user</code> right beside it asks on exactly that flag — the same privilege, granted the same way, with a dialog in front of one of them. The description even advertised it: &quot;which is how you make someone an admin before they first log in.&quot; It is now guarded on the same field and only that field.</p><p><code>chown_repository</code> transfers which forge token a repository&#39;s pipelines run under, so the calling account&#39;s reach over the forge becomes the repository&#39;s reach. <code>delete_user</code> already cited that in its own reasoning; the tool that performs the transfer did not ask.</p></li><li><p><code>ELICITATION</code> switches the dialog off — <code>false</code> sends a client that could have been asked down the two-call-token path instead. For a scheduled job or a test harness, where a dialog is the wrong shape rather than an unwanted one.</p><p>It does <strong>not</strong> remove the guard: there is no setting in which a guarded call goes unannounced. Two deliberate rough edges come with it. The variable is <strong>not prefixed</strong>, so one <code>export ELICITATION=false</code> reaches every MCP server in the environment — which is why a server started with it off prints a line saying so, and why the fallback text names the server instead of blaming a client that was working fine. And a value that is neither <code>true</code> nor <code>false</code><strong>stops the server</strong>, through the same <code>fail()</code> every other refusal in <code>config.ts</code> uses: it is the only variable here that defaults to <em>on</em>. It is read after <code>WOODPECKER_TOKEN</code> is wiped from the environment, so that exit cannot leave the token behind.</p></li><li><p>A <code>docs/guide/approval.md</code> page. <code>test/docs.test.ts</code> compared the 👤 marks in the tool reference against the real <code>confirm_token</code> schemas and failed until the page named the two new tools, which is exactly what it is for.</p></li></ul><h3 id="changed" tabindex="-1">Changed <a class="header-anchor" href="#changed" aria-label="Permalink to “Changed”">​</a></h3><ul><li><p>The advertised schemas avoid a spelling that is legal JSON Schema and still gets a tool refused, or its constraint silently dropped, by some MCP clients: an open object now writes <code>&quot;additionalProperties&quot;: true</code> rather than the empty schema <code>{}</code> zod emits for it. What the tools accept and return is unchanged; only the way the schema says so is.</p></li><li><p>Eight &quot;Nothing to update — pass …&quot; answers are error results rather than plain ones. Each read like an answer while being a refusal.</p></li><li><p>A result too large to shrink is an error rather than an envelope saying so. The envelope was a different shape from what the tool declares it returns, which the SDK refuses.</p></li><li><p>The two-call <code>confirm_token</code> prompt is an error result. What was asked for did not happen, which is what <code>isError</code> says. The text is unchanged and still carries the token.</p></li><li><p><code>move_repository</code> no longer describes the HTTP 500 it answered with as the behaviour of the endpoint. On 3.11 it performed the move and then failed on a permission record with no repository attached; on 3.18 the call answers cleanly. The warning stays, scoped to the older instances it is true of, since a server points at whichever instance it was given.</p></li><li><p>The confirmation prompt is a <strong>plain result rather than an error</strong>. Asking a question is not a failure, and the rest of the family answers it this way.</p></li><li><p>A <code>confirm_token</code> that does not match its arguments is <strong>refused with the reason</strong> in the same words as every other server in the family. The binding is unchanged: a token issued for one repository still cannot authorise the whole-instance variant of <code>repair_repository</code>.</p></li><li><p>Runs on <strong>MCP SDK 2.0</strong>. Existing clients see the same protocol revision they always did; the change is the package layout behind it, and it is what lets the dialog above work on both protocol eras from one code path — including behind a stateless gateway, where the older mechanism silently fell back to the weaker token for every client.</p></li><li><p>The linter is <strong>oxlint</strong> instead of eslint plus typescript-eslint, which lifts the TypeScript ceiling: typescript-eslint pins <code>typescript</code> below 6.1, so this repository was held on TypeScript 6 by its linter rather than by its code.</p></li><li><p>The tool filter, the confirmation store, the host classifier and the documentation-asset generator now come from <strong><code>mcp-tool-allowlist</code></strong>, <strong><code>mcp-approval</code></strong>, <strong><code>mcp-internal-hosts</code></strong> and <strong><code>svg-asset-set</code></strong> rather than from copies kept here — 916 fewer lines, and one place to fix each. None of them has a runtime dependency of its own.</p></li><li><p>stdio is served through <code>serveStdio</code>, so the connection&#39;s era is negotiated on the opening exchange rather than assumed. A client that pins the <code>2026-07-28</code> era is served it; until now its <code>server/discover</code> probe was answered with &quot;Method not found&quot; and only <code>2025-11-25</code> was on offer. A client that speaks the older era sees no change — it is still pinned to one instance for the life of the connection, exactly as a hand-wired <code>StdioServerTransport</code> served it.</p></li></ul><h3 id="fixed" tabindex="-1">Fixed <a class="header-anchor" href="#fixed" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><p>Confirmation tokens are compared with a <strong>constant-time</strong> comparison, in the library&#39;s implementation rather than in the copy kept here.</p></li><li><p><strong><code>budgetedJson</code> could stop the server for good.</strong> It replaced the longest string in a result with 200 characters plus a note saying how many were omitted — 230 characters, which is <em>longer</em> than the 200-character threshold that made it a candidate. So the same slot was picked again, rewritten to the identical text, and the document measured to the identical size, for ever. Any result that shortening alone could not bring under budget reached that fixpoint: <code>get_queue_info</code> on an instance with about four hundred waiting tasks needed no attacker at all, and a <code>.woodpecker.yml</code> with five hundred long step names is a repository&#39;s own choice. Node is single-threaded, so the server then answered nothing — not the tool that triggered it and not any other — until the process was killed.</p><p>Shortened strings are now excluded from the next round by a sentinel, as in <code>wikijs-mcp</code>, so the loop runs out of candidates and falls through to the pass that drops list entries; both passes carry a hard round limit, because a fixpoint must never be a hang. Shortening also happens in doubling batches instead of re-rendering the whole document once per string.</p></li><li><p><strong>A ReDoS in the build-log cleaner.</strong> The rule that keeps only the last state of a line a progress bar rewrote was <code>[^\\n]*\\r</code>: the star runs to the end of the line, finds no carriage return, and backtracks a character at a time, from every start position. Quadratic in the line&#39;s length — 200 000 characters took 10.5 seconds and a megabyte over 48, with the event loop blocked throughout. A single four-megabyte comment line is legal YAML, and <code>get_pipeline_config</code> strips whatever the repository put in <code>.woodpecker.yml</code>, with no limit on how many files it reads. It is now one backwards scan per line.</p><p><code>get_pipeline_config</code> also bounds what it decodes — at most 20 files, at most 200 000 bytes each — and <code>get_step_logs</code> applies its byte budget to each chunk <em>before</em> stripping it rather than to the joined text afterwards.</p></li><li><p><strong><code>get_secret</code> and <code>list_secrets</code> did not remove a secret&#39;s value.</strong> A comment in <code>normalize.ts</code> said they did, and nothing there or in <code>tools/secrets.ts</code> removed anything: the confidentiality of the field rested entirely on Woodpecker&#39;s <code>model.Secret.Copy()</code> stripping it upstream. On a healthy instance that is true, which is why nobody noticed. It is now this server&#39;s own control as well, and the comment describes what the code does.</p></li><li><p><code>WOODPECKER_READ_ONLY</code> is read as <code>1</code>, <code>yes</code> or <code>true</code> in any case, not only as the exact string <code>true</code>. <code>WOODPECKER_READ_ONLY=1</code> — what a Compose file or a systemd unit is most likely to say — used to leave every write tool registered while the operator believed the server could not write. <code>WOODPECKER_INSECURE_TLS</code> stays strict on purpose: a protection should fail on, a permission should not.</p></li></ul><h3 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to “Security”">​</a></h3><ul><li><p><strong>A confirmation authorised the operation nobody was asked about.</strong> The resource key came from <code>setResourceKey</code>, which sorts its targets — correct for a set, wrong for every tool here, whose targets are ordered tuples of small integers that look alike. <code>[&quot;5&quot;,&quot;12&quot;]</code> and <code>[&quot;12&quot;,&quot;5&quot;]</code> hashed to the same key, and the second call&#39;s arguments were never compared with the first&#39;s. A person who read &quot;approve blocked pipeline 12 of repository 5 … runs that fork&#39;s code with this repository&#39;s secrets&quot; and agreed was, with the same token, able to approve pipeline 5 of repository 12: a different fork, and the secrets of a repository that was never mentioned. <code>delete_pipeline</code>, <code>delete_step_logs</code>, <code>delete_cron</code>, <code>delete_agent</code>, <code>delete_user</code> and <code>delete_secret</code> collided the same way.</p><p>Targets now carry their role (<code>repo:5</code>, <code>pipeline:12</code>) and the key preserves their order. Where a call decides more than which object it touches, a fingerprint of the request body is part of the binding too — so a token shown as &quot;grant <code>trusted_network</code>&quot; no longer executes a second call that also carries <code>visibility: &quot;public&quot;</code>. <code>mcp-approval</code> is unchanged; a server whose targets are ordered says so at the call site.</p></li><li><p><strong>Two ungated tools could switch the fork gate off.</strong> <code>approve_pipeline</code> is the most carefully reasoned guard in this server, and <code>update_repository</code> let <code>require_approval: &quot;none&quot;</code> and <code>visibility: &quot;public&quot;</code> through on the first call (only the <code>trusted_*</code> flags were guarded), while <code>update_secret</code> let <code>events</code> and <code>images</code> through (only <code>value</code> was guarded). Together: a fork&#39;s pull request runs unapproved, reads a secret it was newly made visible to, prints it, and the log is world-readable — without a single prompt.</p><p>Both now gate on <em>direction</em>, the way <code>set_log_level</code> and the <code>trusted_*</code> flags already did. Lowering <code>require_approval</code>, making a repository public, adding a <code>pull_request</code> event to a secret or emptying its <code>images</code> list asks first; tightening any of them, and every other field, still applies on the first call. Naming one of those fields costs one extra read, because &quot;lowering&quot; is a comparison with the current setting.</p></li><li><p><strong><code>update_registry</code> rotated an unrecoverable credential without asking.</strong> Its own annotation states the rule — &quot;the old password is not readable through the API and cannot be recovered&quot; — <code>update_secret</code> is guarded by that exact sentence, and <code>delete_registry</code> beside it is two-step for the same damage. Passing <code>password</code> is now two-step; correcting a <code>username</code> alone still applies on the first call. That makes twenty-three guarded operations, not twenty-two.</p></li></ul><h2 id="_0-1-0-2026-08-29" tabindex="-1"><a href="https://github.com/ni-c/woodpecker-ci-mcp/releases/tag/v0.1.0" target="_blank" rel="noreferrer">0.1.0</a> - 2026-08-29 <a class="header-anchor" href="#_0-1-0-2026-08-29" aria-label="Permalink to “0.1.0 - 2026-08-29”">​</a></h2><p>First public release.</p><h3 id="added-1" tabindex="-1">Added <a class="header-anchor" href="#added-1" aria-label="Permalink to “Added”">​</a></h3><ul><li>MCP server for the <a href="https://woodpecker-ci.org" target="_blank" rel="noreferrer">Woodpecker CI</a> API, verified against Woodpecker 3.18.0.</li><li>71 tools covering the whole API: repositories, pipelines, build logs, secrets, registries, cron jobs, organizations, users, agents, forges, and the server queue and log level.</li><li><code>get_step_logs</code> decodes what the API actually returns — an array of base64-encoded chunks — reassembles it in line order, reports the step&#39;s exit code, and returns the end of the log by default, because that is where a failing step explains itself.</li><li>Secrets and registries exist at repository, organization and instance level in Woodpecker; here that is a <code>scope</code> parameter on five tools each rather than fifteen tool names.</li><li><code>WOODPECKER_ALLOW_TOOLS</code> / <code>WOODPECKER_DENY_TOOLS</code> narrow the tool list by name or <code>list_*</code> prefix, and <code>essential</code> selects the eight tools that cover finding a repository, reading its pipelines and logs, and running one again.</li><li><code>WOODPECKER_READ_ONLY=true</code> registers only the 34 read tools; the 37 write tools never reach the client&#39;s tool list.</li><li>Fifteen operations that cannot be undone — every <code>delete_*</code>, plus <code>move_repository</code>, the whole-instance <code>repair_repository</code>, <code>update_forge</code> and <code>pause_queue</code> — are two-step: the first call returns a short-lived confirmation token bound to those exact arguments.</li></ul><h3 id="security-1" tabindex="-1">Security <a class="header-anchor" href="#security-1" aria-label="Permalink to “Security”">​</a></h3><ul><li><strong>Agent tokens are redacted on read.</strong> The Woodpecker API returns each agent&#39;s token in clear text from <code>GET /agents</code>, including the list call, and that token is enough to attach a machine to the server and receive pipeline workloads with their secrets. <code>list_agents</code>, <code>get_agent</code> and <code>update_agent</code> replace it; <code>create_agent</code> still returns it, because that is the only way to get it, and says what it is.</li><li>Build logs, commit messages and pipeline metadata are marked as untrusted content — all of it is written by whoever can push to the repository.</li><li>The access token is deleted from the environment after start-up, never sent to a redirect target, and never echoed into an error message.</li><li>Results are budgeted: list results drop whole entries rather than overflowing the model&#39;s context, and logs are capped separately and far lower. Single objects are budgeted too, by shortening the longest text anywhere in the tree and then dropping entries from the longest list, so an oversized answer comes back smaller rather than not at all.</li><li><strong>Twenty operations are two-step</strong>, not fifteen. <code>approve_pipeline</code> joined them outright — a blocked pipeline is usually one from a fork, approving it runs that fork&#39;s code with the repository&#39;s secrets, and the model deciding whether to approve is typically holding a build log written by whoever opened the pull request. Four more are two-step only in the direction that escalates: <code>update_user</code> granting <code>admin</code>, <code>update_repository</code> granting a <code>trusted_*</code> flag, <code>update_secret</code> overwriting a value that cannot be recovered, and <code>set_log_level</code> silencing the server. The reverse of each, and every other field, still applies on the first call.</li><li><strong>Credential-shaped fields are scrubbed from every pass-through result</strong>, not just the agent token that is known to leak today. Which fields Woodpecker&#39;s Go models serialize is not this server&#39;s decision, and a forge addon or a reverse proxy can reshape a body; a field named <code>password</code>, <code>client_secret</code>, <code>private_key</code> or <code>token</code> is now redacted wherever it appears. <code>create_agent</code> is the one deliberate exception.</li><li><strong>Build output is stripped of terminal control sequences.</strong> A step&#39;s stdout is whatever its container wrote — ANSI colour, cursor movement, progress bars that rewrite their own line, and sometimes raw binary. None of it survives as meaning in a JSON result, all of it costs budget, and the escape sequences are a rendering vector in whatever terminal displays the answer.</li><li>The invariant that no attacker-controlled prose reaches a confirmation prompt is now enforced where the interpolation happens, rather than resting on three input-schema regexes two files away.</li><li>Repository-controlled text carries the untrusted-content marker in every result that returns it, including the pipeline echoed back by <code>trigger_pipeline</code>, <code>restart_pipeline</code>, <code>approve_pipeline</code>, <code>decline_pipeline</code> and <code>run_cron</code>, the instance-wide pipeline queue, and forge branch names.</li><li>The fatal-error handler prints the message and stack rather than the error object, whose <code>cause</code> chain can carry the failed request&#39;s headers.</li></ul>`,16)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};