import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.Cs5aHXsR.js";var i=JSON.parse(`{"title":"Configuration","description":"","frontmatter":{},"headers":[],"relativePath":"guide/configuration.md","filePath":"guide/configuration.md","lastUpdated":1788342797000}`),a={name:`guide/configuration.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to “Configuration”">​</a></h1><p>Everything is an environment variable; there is no config file. The <a href="/reference/environment">environment reference</a> has the full table.</p><h2 id="the-token" tabindex="-1">The token <a class="header-anchor" href="#the-token" aria-label="Permalink to “The token”">​</a></h2><p><code>WOODPECKER_TOKEN</code> is a personal access token from <strong>User settings → CLI and API</strong>. Woodpecker has no token scopes: the token carries the full authority of the account it belongs to, and there is no read-only variant to reach for. <code>get_current_user</code> reports which account is configured and whether it is an instance administrator.</p><p>The token is read once at start-up and then deleted from the process environment, so a later crash report or a child process cannot pick it up. It is never written to a log, never included in an error message, and never sent to a redirect target — every request is made with <code>redirect: &quot;error&quot;</code>, because following a redirect would hand the <code>Authorization</code> header to whatever host the upstream named.</p><h2 id="the-url" tabindex="-1">The URL <a class="header-anchor" href="#the-url" aria-label="Permalink to “The URL”">​</a></h2><p><code>WOODPECKER_URL</code> is the server root — <code>https://woodpecker.example.com</code> — not the API root. A trailing <code>/api</code> is trimmed rather than refused, because that is how the Swagger page spells its examples.</p><p>A URL that contains credentials (<code>https://user:pass@host</code>) is refused outright: it would end up in logs. A URL that is not http or https is refused too. Plain http to a non-local host is allowed but warns, because the token then travels in the clear.</p><h2 id="read-only-mode" tabindex="-1">Read-only mode <a class="header-anchor" href="#read-only-mode" aria-label="Permalink to “Read-only mode”">​</a></h2><p><code>WOODPECKER_READ_ONLY=true</code> registers the 34 read tools and none of the 37 write tools. They are not rejected at call time — they are never registered, so they do not appear in <code>tools/list</code> and a call to one is answered with &quot;tool not found&quot;, exactly as if the server did not have it.</p><p>This is the switch to reach for by default. Everything that reads a pipeline, a log, a secret&#39;s metadata or an agent&#39;s state stays available.</p><h2 id="admin-only-tools" tabindex="-1">Admin-only tools <a class="header-anchor" href="#admin-only-tools" aria-label="Permalink to “Admin-only tools”">​</a></h2><p>Woodpecker inherits repository and organization permissions from the forge, and reserves users, agents, forges, the queue and the log level for instance administrators. Those tools are registered for every account and answer 403 for one that may not use them.</p><p>If your token is not an admin token, <code>WOODPECKER_DENY_TOOLS</code> is the tidy way to stop offering the tools that can only fail:</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">WOODPECKER_DENY_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">list_users,get_user,create_user,update_user,delete_user,</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">\\</span></span>
<span class="line"><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">list_agents,get_agent,list_agent_tasks,create_agent,update_agent,delete_agent,</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">\\</span></span>
<span class="line"><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">list_forges,get_forge,create_forge,update_forge,delete_forge,</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">\\</span></span>
<span class="line"><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">get_queue_info,get_log_level,set_log_level,pause_queue,resume_queue</span></span></code></pre></div><h2 id="tls-and-self-hosted-instances" tabindex="-1">TLS and self-hosted instances <a class="header-anchor" href="#tls-and-self-hosted-instances" aria-label="Permalink to “TLS and self-hosted instances”">​</a></h2><p><code>WOODPECKER_INSECURE_TLS=true</code> accepts a self-signed certificate. It is scoped to this server&#39;s connection through a dedicated undici dispatcher — it does not disable certificate validation process-wide, and <code>NODE_TLS_REJECT_UNAUTHORIZED</code> is never touched.</p><p>Prefer an internal CA your machine trusts. An MCP client typically runs on a laptop that also talks to the public internet, and a process-wide hole is a hole in all of it.</p><h2 id="turning-the-approval-dialog-off" tabindex="-1">Turning the approval dialog off <a class="header-anchor" href="#turning-the-approval-dialog-off" aria-label="Permalink to “Turning the approval dialog off”">​</a></h2><p>The twenty-three irreversible operations ask a person through MCP elicitation before they act. <code>ELICITATION=false</code> takes them to the two-call token instead. It does not remove the guard; there is no setting in which a guarded call goes unannounced.</p><p>The variable deliberately carries no <code>WOODPECKER_</code> prefix, which means it reaches every MCP server in the same environment, and — unlike <code>WOODPECKER_READ_ONLY</code> — a value it does not recognise <strong>stops the server</strong> rather than failing off. See <a href="/guide/approval">Asking a person</a>.</p><h2 id="choosing-the-tools-that-load" tabindex="-1">Choosing the tools that load <a class="header-anchor" href="#choosing-the-tools-that-load" aria-label="Permalink to “Choosing the tools that load”">​</a></h2><p><code>WOODPECKER_ALLOW_TOOLS</code> decides what is registered; <code>WOODPECKER_DENY_TOOLS</code> is subtracted from whatever is left. Both take a comma-separated list of tool names, a prefix with a single trailing <code>*</code>, or the word <code>essential</code>.</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6E7781;--shiki-dark:#8B949E;"># The curated eight: find a repository, read its pipelines and logs, run one again</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">WOODPECKER_ALLOW_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">essential</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6E7781;--shiki-dark:#8B949E;"># Everything that reads, plus one that acts</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">WOODPECKER_ALLOW_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">list_*,get_*,restart_pipeline</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6E7781;--shiki-dark:#8B949E;"># Everything except the ones that cannot be undone</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">WOODPECKER_DENY_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">delete_*,pause_queue,set_log_level</span></span></code></pre></div><p>With 71 tools this is not decoration. Every registered tool costs context on every request, and a model picks the right one far more reliably from eight than from 71. <code>essential</code> is <code>list_repositories</code>, <code>get_repository</code>, <code>list_pipelines</code>, <code>get_pipeline</code>, <code>get_step_logs</code>, <code>trigger_pipeline</code>, <code>restart_pipeline</code> and <code>cancel_pipeline</code> — marked as <strong>essential</strong> in the <a href="/reference/tools">tool reference</a>.</p><p>Two behaviours worth knowing:</p><ul><li><strong>An entry that matches no tool stops the server</strong>, names the entry and lists the real tool names. It does not warn and carry on, because the alternative failure is invisible: a tool quietly missing from <code>tools/list</code>, which nobody traces back to an environment variable.</li><li><strong>A filtered tool does not exist</strong>, rather than existing and refusing. It is absent from <code>tools/list</code> and unknown to <code>tools/call</code>, which is the same thing a write tool under <code>WOODPECKER_READ_ONLY</code> does.</li></ul><p>A pattern is a prefix plus exactly one trailing <code>*</code>. <code>list_*</code> is valid; <code>*_pipeline</code> and <code>list_*_x</code> are refused, because they look plausible, match nothing and would otherwise be silent forever.</p><p>If a pattern matches only write tools while <code>WOODPECKER_READ_ONLY</code> is set, the server warns and continues — a pattern is a template. An exact name in that situation is an error, because someone typed it believing it would be there.</p>`,29)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};